apiVersion: v1
kind: Service
metadata:
  name: inventory-service
spec:
  type: LoadBalancer # Cria um ponto de entrada público
  selector:
    app: inventory-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000